import React, { useState , useEffect, useContext} from "react";

const UnreadMsg = ({room}) => {

    const { state: authState , dispatch } = useContext(authContext);

const messages[0].createdAt
const users[0].online
//filter que les msg qui sont apres la date de online
//soit ts les msg > a online


    return(
        <div>

        </div>
    )
}

export default UnreadMsg;